download node then run :
npm install	(Installs all dependencies)
npm run dev	(Starts development server)
npm run build	(Builds the project for production)
npm start	(Runs the built production server)
